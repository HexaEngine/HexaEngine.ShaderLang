namespace Core
{
	using Math;

	enum TestEnum
	{
		First,
		Second = 1 << 3,
		Third = Second | 1 << 4,
	}

	struct TestStruct
	{
		int a;
		float b;
		TestEnum c;
	}

	float test(float x)
	{
		int a = 5;
		for (int i = 0; i < 3; i++)
		{
			a += i;
		}
		return pow3(a) * x + x * pow3(a);
	}
}