namespace Core;

float pow2(float x)
{
	return x * x;
}

float test(float x)
{
	float a = pow2(10f);
	for (int i = 0; i < 10; i++)
	{
		for (int j = 0; j < 10; j++)
		{
			for (int k = 0; k < 10; k++)
			{
				a += pow2(x + i + j + k);
			}
		}
	}
	return a;
}
