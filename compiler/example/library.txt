namespace Core;

struct MyStruct
{
	float value0;
	float value1;
	float value2;
}

float pow2(float x)
{
	return x * x;
}

float test(float x, float y)
{
	float a;
	a = 10;
	a = pow2(a);
	return x * 80 + x * 80 + x * 80 * a;
}
