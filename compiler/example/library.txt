

namespace Math
{
	float pow2(float x)
	{
		return x * x;
	}

	float pow3(float x)
	{
		return pow2(x) * x;
	}
}

namespace Core
{
	using Math;

	float test(float x)
	{
		int a = 5;
		for (int i = 0; i < 3; i++)
		{
			a += i;
		}
		return pow3(a) * x;
	}
}