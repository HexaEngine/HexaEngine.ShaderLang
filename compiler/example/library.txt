namespace Core;

struct MyStruct
{
	float value0;
	float value1;
	float value2;
}

float pow2(float x)
{
	return x * x;
}

float test(float x, float y)
{
	float a;
	if (x > y)
	{
		a = x + y;
	}
	else
	{
		a = x - y;
	}
	a = pow2(a);
	return x * 80 + x * 80 + x * 80 * a;
}
