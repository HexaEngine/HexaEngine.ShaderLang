namespace Core;

struct MyStruct
{
	float value0;
	float value1;
	float value2;

	MyStruct(float value1_m)
	{
		value1 = value1_m;
	}
}

float test(float x, float y)
{
	MyStruct a = MyStruct(y);
	a.value1 = y;
	return x * 80 + x * 80 + x * 80 * a.value1;
}
